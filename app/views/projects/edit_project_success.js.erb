$("#<%= @project.id %>-project").remove();
$('#<%= @project.id %>-modal').modal('toggle');

$('#project-list').append("<%=  j render 'project', {p: @project} %>");
$('#<%= @project.id %>-modal input:not([type="submit"])').each(function(){
    $(this).val("");
});
